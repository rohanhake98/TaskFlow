# Project Reset

This project has been reset to a minimal state to start development from scratch.

## Current Setup
- **Frontend:** React 18, Vite, TypeScript, Tailwind CSS, Shadcn/UI (installed but unused).
- **Backend:** None (Cleared).
- **Database:** None (Cleared).

## Getting Started
1. Install dependencies:
   ```bash
   npm install
   ```
2. Start the development server:
   ```bash
   npm run dev
   ```
